<footer class="footer">
  <div class="shell footer-inner">
    <div class="footer-content">
      <p class="text-sm text-muted">
        Carlos Paramio <span class="footer-divider" aria-hidden="true">·</span> Software & Cloud Architect
      </p>
      <nav class="footer-links" aria-label="Social links">
        <a href="https://github.com/carlosparamio" class="footer-link" target="_blank" rel="noopener me">GitHub</a>
        <a href="https://linkedin.com/in/carlosparamio" class="footer-link" target="_blank" rel="noopener me">LinkedIn</a>
        <a href="https://twitter.com/carlosparamio" class="footer-link" target="_blank" rel="noopener">Twitter</a>
      </nav>
    </div>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--color-border);
    margin-top: var(--space-24);
  }

  .footer-inner {
    padding-block: clamp(2.5rem, 4vw + 1rem, 3.5rem);
  }

  .footer-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: clamp(1rem, 4vw, var(--space-8));
    flex-wrap: wrap;
  }

  .footer-links {
    display: flex;
    gap: clamp(1rem, 3vw, var(--space-6));
  }

  .footer-divider {
    opacity: 0.5;
    margin: 0 var(--space-2);
  }

  .footer-link {
    position: relative;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .footer-link::after {
    content: '';
    position: absolute;
    inset-inline: 0;
    bottom: -0.35rem;
    height: 1px;
    background: var(--color-accent);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform var(--transition-fast);
  }

  .footer-link:hover,
  .footer-link:focus-visible {
    color: var(--color-accent);
  }

  .footer-link:hover::after,
  .footer-link:focus-visible::after {
    transform: scaleX(1);
  }

  @media (prefers-reduced-motion: reduce) {
    .footer-link,
    .footer-link::after {
      transition: none;
    }
  }

  @media (max-width: 640px) {
    .footer-content {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--space-4);
    }

    .footer-links {
      gap: var(--space-4);
    }
  }
</style>
