services:
  dev:
    build: .
    user: "1000:1000"
    ports:
      - "4321:4321"
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - HOST=0.0.0.0
    stdin_open: true
    tty: true

volumes:
  node_modules:
